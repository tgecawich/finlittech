name: CI

on:
  push:
  pull_request:

# A superseded run tells you nothing useful, and the free tier is finite.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: typecheck, lint, test, build
    runs-on: ubuntu-latest

    steps:
      # v5 of both actions runs on the Node 24 runtime. v4 targets Node 20,
      # which GitHub has deprecated and currently force-upgrades with a warning.
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: npm

      - run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      # Runs the suite and enforces the 100% branch threshold on lib/finance.
      # Deliberately not `npm test`: passing tests with an unexercised branch is
      # the failure mode this project cares about.
      - name: Test with coverage
        run: npm run coverage

      - name: Build
        run: npm run build
